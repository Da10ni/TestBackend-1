# Normalize line endings across platforms.
* text=auto eol=lf

# Shell scripts MUST stay LF — CRLF breaks the interpreter inside Linux containers.
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf
